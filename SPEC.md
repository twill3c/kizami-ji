# SPEC.md — kizami-ji(刻み路)

<!-- scaffold template v1.13.0 から展開(2026-08-30)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

形態素解析は決まった答えを返す処理に見えるが、実体は**ラティス上の最小コスト経路探索**である。
本アプリは、割れた結果ではなく**割れ方が何によって決まったか**(候補ノード・生起コスト・連接コスト・
最小経路・二位との差 δ)を静的サイト上で見せる。

対になる既存アプリは token-hakari(トークン秤)。あれは*統計だけで刻む*(BPE)話であり、
本アプリは*人が定めた辞書と文法コストで刻む*話である。app-menu の cat10「AI・言語モデル」に置く。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 入力文からラティス(候補ノード集合)を構成し、生起コスト・左右文脈 ID つきで画面に出す | must |
| F-02 | Viterbi による最小コスト経路を求め、経路コストの内訳(Σ生起 / Σ連接 / EOS)を出す | must |
| F-03 | DP が位置ごとに埋まっていく過程を段階表示する(教材としての中核) | must |
| F-04 | N-best を求め、一位と二位の差 **δ** を表示する。二位の道をラティス上に重ねる。**δ を「危うさ」「間違いやすさ」と結びつけて呼ばない**(G-01 不通過・F-32b) | must |
| F-05 | 未知語ノードを `char.def` 由来の文字種規則で生成し、辞書語と区別して表示する | must |
| F-06 | トップページは辞書を **1 バイトも取得しない**。短い例文の解析結果を事前計算して見せる | must |
| F-07 | 「**僅差の文**」ページ: δ の分布と、同点・僅差の実例一覧。δ = ∞ の割合も出す | must |
| F-08 | 初回解析までの**実測**転送バイト数を画面に表示する | must |
| F-09 | 「方法と限界」ページ: 照合結果・G-01 の数字・IPADIC の年代・δ で言えないこと | must |
| F-10 | 語彙を刈った辞書での解析比較(「痩せた辞書」) | should |
| F-11 | フリート共通フッタ規約 5 項目 | must |

### F-04a δ の定義(循環を避けるための明文化)

δ = (二位の経路コスト) − (一位の経路コスト)。ここで**二位は一位と分割が異なる経路**とする
(同じ分割で異なる同表層トークンを選んだだけの経路は二位に数えない)。δ = 0 は**完全同点**を意味し、
どちらが選ばれるかは辞書の走査順(タイブレーク)で決まる。

### F-32 出さないもの(測ってから決める枠)

以下は**較正で残った場合にのみ**出荷する。落ちたら SPEC に理由を残して落とす。

| ID | 機能 | 出荷条件 |
|---|---|---|
| F-07 | δ を「危うさ」として提示すること | **G-01 不通過につき出荷しない**(下記 F-32b) |
| F-10 | 痩せた辞書 | G-03 を侵さない範囲で事前計算できること |

### F-32b δ を「危うさ」と呼ばない(G-01 不通過による決定・2026-08-30)

G-01 は落ちた。Q1/Q4 の違反率比 **1.193**(閾値 1.5)、Spearman **−0.0295**。
さらに漢字連続の内部に限ると効果が**反転**する(比 0.820・ρ +0.0268)——
全体で見えたわずかな効果は「漢字らしさ」の影である(Q1 の 22.4% 対 Q4 の 11.3%)。
測定の途中でルビの物差しを自動推定から `｜` の明示に替えたが、**どちらでも落ちる**
(自動推定版は比 1.057・漢字内で 0.503)。詳細と実例は `docs/g01-findings.md`。

- δ は出す。ただし**「解析器が迷った量」という内部量**としてのみ提示する
- 「危うい」「間違いやすい」「正しい/正しくない」と δ を結びつける語を**出荷物の本文に置かない**
  (T-034 が禁止語検査で守る。陽性対照つき)
- 「危うい文」ページは「**僅差の文**」に改める。同点・僅差が**実在すること**は測れており、そこは主張として立つ
- 代わりに測れた事実を出す: **全境界の 83.0% は δ = ∞** —— 跨ぐ語が辞書に一つも無い。
  分割の大半は迷った末の判断ではなく、語彙がそこにしか道を残していないという事実の反映である

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Next.js `output: "export"` の静的サイト。サーバ関数・cron・API 鍵ゼロ | ビルド成果物に関数が無いことを検査 |
| N-02 | 実行時の外部通信ゼロ。入力文はどこへも送らない | 実ブラウザで通信ログを取り、自オリジン以外が 0 件 |
| N-03 | 解析は閲覧者の端末で走る TypeScript 実装。WASM を使わない | 出荷物に `.wasm` が無いこと |
| N-04 | 初回解析までの転送量 8 MB 未満(G-03) | 実ブラウザで実測(HEAD ではなく範囲つき GET) |
| N-05 | トップページの辞書取得は 0 バイト(F-06) | 実ブラウザで実測 |
| N-06 | コーパスは aozora-sakuin の `data/normalized` を再利用し、**再取得しない** | 取得スクリプトを置かない |
| N-07 | mecab-ipadic の著作権表示を NOTICE に置く | ファイル存在と本文の検査 |
| N-08 | 参照実装(fugashi/MeCab)はビルド時と検査のみ。出荷物に含めない | O-2 の静的検査 |

## 4. 品質基準(オラクルと較正ゲート)

**閾値はすべて測る前に置く。落ちたときは実装を疑い、直せないなら主張のほうを捨てる。**

| ID | 内容 | 閾値 | 状態(loop_005 時点) |
|---|---|---|---|
| O-1a | MeCab(fugashi + ipadic)と**総コスト**が一致 | 100.0000% | **達成**(10,000 文・不一致 0)T-010 |
| O-1b | MeCab と**分割**が一致(タイブレーク規則を合わせた上で) | 100.0000% | **達成**(10,000 文・不一致 0)T-010 / 外挿 5,000 文も 0 T-011b |
| O-2 | 出荷実装が参照実装を参照していない(静的検査) | 違反 0 | **達成** T-021〜T-023(陽性 3・陰性 2 対照つき) |
| O-3 | 連接表をシャッフルした辞書では O-1a が崩れる(陰性対照) | 有意に崩れる | **達成** T-020 |
| G-01 | δ とルビ境界違反の相関(外部の物差し) | 比 1.5 以上 | **不通過**(1.193)→ 主張を捨てた。`docs/g01-findings.md` |
| G-02 | 未知語の文字種判定が参照と一致 | 完全一致 | **達成**(3,000 文中の未知語含有文)T-013 |
| G-03 | 初回解析までの実測転送バイト数 | 8 MB 未満 | **達成 5.11 MB**(実ブラウザ・gzip 転送の符号化後)T-043 |

O-1a / O-1b は **TypeScript の出荷実装でも再現した**(T-014)。2,000 文のフィクスチャ(コミット済み)に加え、
Python と同じ 10,000 文(seed 20260829)で総コスト・分割・未知語判定すべて不一致 0 件。
10,000 文の解析に **1,340 ms**(1 文あたり 0.13 ms)。

O-2 の静的検査は **禁止語を原稿に直接書かず符号位置から組み立てる**。書くと原稿の走査が
自分の対照を撃つ(次の一字 HC-002 と同型)。

### O-1a / O-1b を分けた理由

当初案は「分割の 100% 一致」を単独の閾値にしていた。先行検証(§6)で**完全同点が実在する**と
分かったため、SPEC に書く前に二つへ割った。そのままなら、達成不能な閾値を掲げて実装を疑い続けることになっていた。
O-1b は「タイブレーク規則を MeCab に合わせる」を実装要件に格上げして初めて成立する。

### G-01 の設計(循環の禁止)

δ は IPADIC の内部量である。それを IPADIC の分割で採点すれば、辞書が自分の自信を自分で採点することになる。
外部の物差しとして**青空文庫のルビ**を使う。ルビが振られた漢字列は、人間がそこをひとまとまりと見た証拠であり、
辞書とは無関係に存在する(抽出器は furigana-keiryo の実装を再利用する)。

- 指標: 形態素境界が**ルビの括りの内部**に落ちた箇所を「境界違反」とする
- 主張: δ が小さい箇所ほど境界違反率が高い
- **閾値(測る前に置く)**: δ の下位四分位と上位四分位で違反率の比が **1.5 倍以上**、
  かつ順位相関(Spearman)の符号が負で **α = 0.05** で有意
- **対照**: δ をシャッフルした帰無分布(seed 20260830・2,000 回)で経験 p を取る
- **落ちたら**: F-07 と F-04 の「危うさ」という言い方を捨て、δ は「解析器が迷った量」という
  内部量としてのみ提示する(値は出すが、正しさとは結びつけない)

#### 単位の確定(loop_004・**測る前に書いた**)

上の閾値は動かさない。曖昧だった「箇所」の定義だけを、測定に入る前に固める。

- **単位は文ではなく、最小経路が引いた一本の内部境界**である。
  文単位の δ にすると、長い文ほど必ずどこかに僅差があり、**δ が文長の関数になる**
  (標本サイズ交絡 — 猫アトラス HC-025 と同型)。境界単位ならこの交絡が入らない
- **δ は局所量として定義する。** 位置 p における
  `δ_local(p) = (p をまたぐノードを含む最良経路のコスト) − (最小コスト)`。
  δ_local(p) = 0 は「p で切らなくても同じ値の道がある」を意味する。
  p をまたぐノードが辞書に一つも無いときは δ_local(p) = ∞(ラティスが強制した境界)
- **母集団は「最小経路の内部境界」全体**、ラベルは「その境界が 2 文字以上のルビの括りの
  内部に落ちているか」。ルビ 1 文字の括りは内部に境界を持ちえないので母集団から外す
- ルビ側は**形態素解析を一切使わずに**抽出する(T-033)。基底の推定は文字クラスの連続のみ

#### 交絡の検査(閾値ではなく、必ず報告する項目)

δ が小さい境界は**漢字の連続の内部に多い**(漢字熟語は辞書に載っているので跨ぐ候補が出る)。
ルビもまた漢字に付く。したがって「δ が小さいほどルビを跨ぐ」は、**両方が漢字らしさの
関数であるだけ**でも成立しうる(猫アトラス HC-025 と同型の交絡)。

- 四分位ごとに「境界が 2 文字以上の漢字連続の内部にあるか」の割合を出す
- **漢字連続の内部に限った部分集団**でも同じ検定を行う
- 主たる合否は事前登録どおり母集団全体で判定するが、**部分集団で効果が消えるなら
  そのことを画面と本文に書く**。消えた効果を書かずに全体の数字だけ出してはならない

### 循環の禁止(構造)

- 出荷実装は参照実装(fugashi/MeCab)の出力を一切 import しない。O-2 が静的検査する。
  **実装は AST 解析ではなく、コメントを除いたうえでの字句走査**(`build/circularity.mjs`)。
  import 文だけでなく文字列リテラル中の名指しも捕まえる代わりに、識別子の由来までは追わない
- δ の集計は出荷実装だけで作る。参照実装は検算専用
- G-01 のルビ側は形態素解析を使わずに抽出する

### 陽性・陰性対照(HC-041 / HC-051)

- O-3 が O-1a の陰性対照(連接表シャッフル)
- G-01 の対照は δ シャッフル
- 検出系のテストには、捕まえるべき悪い例と撃ってはならない正当な例を同じ場所に置く

## 5. データと辞書

| 項目 | 値 | 出所 |
|---|---|---|
| 辞書 | mecab-ipadic 2.7.0-20070801(pip `ipadic` 1.0.0) | 実測 2026-08-29 |
| 語彙 | 392,126 語 / 表層 325,871 種 | 実測(§6) |
| 連接 ID | 1,316 × 1,316 | 実測 |
| 文字種 | 11 カテゴリ(DEFAULT/SPACE/KANJI/SYMBOL/NUMERIC/ALPHA/HIRAGANA/KATAKANA/KANJINUMERIC/GREEK/CYRILLIC) | 実測 |
| コーパス | 青空文庫 5,000 作(`../aozora-sakuin/data/normalized`) | 再利用・再取得しない |

### 出荷する部位・しない部位

`sys.dic` 46.92 MB の三分の二は読み・発音・活用の文字列であり、本アプリは使わない。

| 部位 | 生サイズ | 出荷 |
|---|---|---|
| double-array(索引) | 10.90 MB | **配らない** — 表層を整列して前方一致で引く自前索引に組み直す |
| token(連接 ID・品詞・生起コスト) | 5.98 MB | 列指向で配る |
| feature(読み・発音・活用形) | 30.04 MB | **配らない** |
| matrix(連接コスト表) | 3.30 MB | 配る |
| char.bin / unk.dic | 0.26 MB | 配る |

品詞名は `token.posid`(69 種)から引く小さな表を別に持つ。`posid` が品詞四つ組を一意に決めることは
build 時に検算する(実測: 69 種・矛盾 0)。活用型/活用形は 388 対しかないので id 化して残す。

### 組み直した結果(loop_002 実測 2026-08-30)

`scripts/build_dict.py` が `data/dict/` に書き出す。**double-array は捨て、整列した表層列への
前方一致(二分探索の入れ子narrowing)に置き換えた。** 索引を持たないぶん小さく、圧縮も効く。

**形式 2**(loop_008)。**導出できるものを配らない。**

| ファイル | 生 | gzip |
|---|---|---|
| `surf.bin`(表層を整列して連結) | 3.40 MB | 1.04 MB |
| `matrix.bin`(連接表) | 3.30 MB | 1.61 MB |
| `tok_lc/rc/cost/cf/pos` | 計 3.37 MB | 計 0.69 MB |
| `surf_len.bin` / `tok_count.bin`(u8 の長さ) | 各 0.31 MB | 0.08 / 0.04 MB |
| `chars.bin` / `meta.json` | 0.03 MB | 0.01 MB |
| **合計** | **10.72 MB** | **3.48 MB** |

形式 1 では絶対位置を u32 で配っていた(`surf_idx.bin` `tok_start.bin`・各 1.24 MB)。
**`tok_start` は `tok_count` の累積和と完全に一致していた** —— 配る必要がまったく無かった。
`surf_idx` も差分を取れば表層の長さ(最大 78)で、u8 に収まる。
どちらも読み込み時に累積和で作り、**総和が実体と合うことを両実装で検算する**(HC-003)。

G-03 の 8 MB に対して gzip だけで 4.27 MB。実ブラウザ(ローカル配信・gzip)で 5.11 MB、
**本番で 5.38 MB**(`build/page_check.mjs --url`・2026-08-30 実測)。

### Brotli のほうが大きかった(loop_007 実測)

SPEC はここに「Vercel は Brotli を掛けるのでさらに下がりうる」と書いていた。**逆だった。**

| | 生 | Brotli | gzip |
|---|---|---|---|
| 合計 | 12.89 MB | **5.37 MB** | **4.30 MB** |
| `surf_idx.bin` | 1.24 MB | 0.99 MB | 0.45 MB |
| `tok_start.bin` | 1.24 MB | 0.87 MB | 0.43 MB |

ブラウザは `br` を先に出すので、閲覧者は**大きいほう**を受け取っている。
差が大きいのは `surf_idx.bin` と `tok_start.bin` —— どちらも**単調増加の u32 配列**で、
Vercel の Brotli 品質設定では deflate に負ける。

**圧縮器を責める前に、データのほうを直すのが筋である。** loop_008 で形式 2 に改めた結果:

| | 形式 1 | 形式 2 |
|---|---|---|
| 生 | 12.89 MB | **10.72 MB** |
| ローカル検品(gzip) | 5.11 MB | **3.51 MB** |
| 本番(Brotli) | 5.38 MB | 下記のとおり実測 |

転送量は**符号化後のバイト数**で測る。`response.body()` は伸長後の中身を返すので、
それを転送量と呼ぶと 12.89 MB になって判定が意味を失う(loop_005 で一度そう報告して落とした)。

### F-02a タイブレーク規則(O-1b の実装要件)

MeCab は `end_node_list` へ**先頭挿入**し、経路の比較を**厳密な `<`** で行う。したがって同点では
**リストの先頭にあるノード**が勝つ。先頭に来るのは最後に挿入されたもの ——
すなわち**最も遅く始まったノード**であり、同じ開始位置の中では**最初に生成されたトークン**
(辞書語 → 未知語の順、同表層なら token 順)。出荷実装はこれを再現する。

この規則は最初に見つかった 7 件の同点を見て決めたので、**別の種・別の作品 5,000 文で外挿を確かめた**
(T-011b)。同じ標本で合わせた規則を同じ標本で検証すれば循環する。

## 6. 先行検証(loop_000 相当・probe/ 配下)

**リポジトリ作成前にスクラッチパッドで実施した。以下の数値は probe 由来であり、
本流実装で再現するまで「未再現」である(onomato-atlas HC-001)。**

実施日 2026-08-29。青空文庫 400 作から抽出した 10,000 文(乱数種 20260829)。
IPADIC のバイナリのみを読む独立実装 `probe/ref.py` と MeCab を照合した。

| 測ったもの | 結果 | 再現状況(loop_002) |
|---|---|---|
| 総コストの不一致 | 0 / 10,000 | **再現**(組み直した辞書で T-010) |
| 分割の不一致 | 7 / 10,000 | **再現後に解消** — タイブレーク規則(F-02a)を入れて 0 件 |
| うち完全同点 | 7 / 7 | **再現**。ただし下記のとおり測り方が変わった |
| 経路が見つからなかった文 | 0 | **再現** |
| double-array から復元した表層 | 325,871 種 | **再現**(T-001b で集合一致) |
| 復元トークン総数 ÷ lexsize | 392,126 / 392,126 | **再現**(T-001) |

**タイブレークを揃えた結果、同点は「MeCab との食い違い」としては観測できなくなった。**
以後、同点は δ = 0 として N-best から直接測る(F-04a)。probe の 7 件は
「二つの実装が違う側に倒れた同点」だけを数えており、はじめから下限だった。
δ による本数の計測は loop_004 以降で行う。

未知語生成を入れる前は不一致 28 件で、その全部がカタカナ語のまとめ上げだった。
`char.bin` から復元した `invoke/group/length` は IPADIC 同梱 `char.def` と値まで一致した
(KATAKANA `1 1 2` / KANJI `0 0 2` / HIRAGANA `0 1 2`)。

### 同点の実例(全経路列挙・**Python / TypeScript の両方で再現済み** T-012)

「の北詰まで」全 1,975 経路のうち上位二つが **12,965** で並ぶ。

| 道 | Σ生起 | Σ連接 | EOS | 総計 |
|---|---|---|---|---|
| の / 北詰 / まで | 14,218 | −1,995 | +742 | 12,965 |
| の / 北 / 詰 / まで | 17,321 | −5,098 | +742 | 12,965 |

生起コストの差 3,103 を連接コストの差 3,103 がちょうど打ち消している。
出典は永井荷風「地下鉄道は既に京橋の北詰まで開鑿せられ」。

## 7. 画面

| ページ | 内容 | 状態 |
|---|---|---|
| トップ | 「の北詰まで」のラティスと道(**焼いた図**)。同点の実例。コーパス実測の要点 | loop_006 |
| 刻む | 自由入力。ラティス・DP の進み・二位の道・コスト内訳・境界ごとの δ・候補一覧・転送量 | loop_005 |
| **僅差の文** | δ の分布(帯ごと)と、完全同点の実例一覧。**「危うい文」から改名**(F-32b) | loop_006 |
| 方法と限界 | G-01 が落ちた顛末・交絡・物差しを替えた記録・MeCab との一致・言えないこと | loop_006 |

**「刻む」以外の 3 ページは辞書を 1 バイトも読まない。** トップの図も僅差の文の表も
方法と限界の数字も、すべてビルド前に焼いてある(`scripts/measure_delta.py`・
`scripts/make_top_figure.mts`)。焼いた図が実装とずれていないことは、毎回作り直して照合する
(`tests/ts/topfigure.test.ts`)。

### コーパス実測(loop_006・2026-08-30)

青空文庫 60,000 文 / **1,278,250 本の境界**(乱数種 20260903・6〜80 字)。

| | 値 |
|---|---|
| δ = ∞(跨ぐ語が辞書に一つも無い) | **1,045,976(81.8%)** |
| δ = 0(完全同点) | **70 本** |
| 同点を含む文 | 67 / 60,000(**およそ千文に一つ**) |
| 同点の種類(切り方の組) | 26 通り |

## 8. スコープ外

- **UniDic 版の出荷**。連接表が 5,981² で生 68.2 MB あり配れない(実測 2026-08-29)。
  UniDic は検証側の第二の物差しとしてのみ使う可能性を残すが、本ループでは扱わない
- **係り受け解析・品詞体系の教育的解説**。本アプリは「刻む」までを扱う
- **辞書の学習・コスト推定**。IPADIC の重みを所与とする
- **読み・発音の表示**。feature を配らないため原理的にできない。画面にその旨を書く
- **語釈の生成**。測ったものと照合できないため出さない(onomato-atlas と同じ判断)
- **新語・固有名への対応**。IPADIC は 2007 年で止まっており、近年の語は未知語になる。
  これは欠陥ではなく前提として画面に書く
