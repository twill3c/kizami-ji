# HARNESS_CHANGELOG.md — ハーネス改訂台帳(kizami-ji)

原則: **エージェントがミスをするたびに、そのミスが二度と起きないようハーネスを改良する。**
起票条件: 同一失敗コード累計 2 回(LL-10)、または severity S1(LL-12)。

各エントリは「どの失敗が、どの文書のどの改訂を生み、効いたかをどう確認するか」を 1 レコードで残す。
**HC-001〜006 は 2026-08-31 に人間の判断でフリートへ昇格し、HC-073〜078 として
`harness-kit/scaffold-kit/registry/blocks/agents_core.md` に収録された**(registry v1.22.0)。
以後この 6 件の正本はレジストリ側にあり、ここは起票の記録として残す。

---

## HC-001(→ フリート HC-073)

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-30 |
| トリガー | 先行検証(loop_000 相当)で「達成不能な閾値」を SPEC に書きかけた |
| 診断 | 二実装照合のオラクルに「分割の 100.0000% 一致」を単独の閾値として置こうとしていた。しかし最小コスト経路には**完全同点が実在する**ため、タイブレーク規則を揃えない限りこの閾値は原理的に達成できない。実測 10,000 文で不一致 7 件、その全部が同点だった。先行検証をしていなければ、達成できない閾値を掲げたまま実装を疑い続けていた |
| 改訂 | SPEC §4 の O-1 を **O-1a(総コストの一致)/ O-1b(分割の一致・タイブレーク規則を合わせた上で)** に分割。あわせて TEST_SPEC に T-012(同点の実例)を追加 |
| 一般化 | **オラクルの閾値を置く前に、二つの実装が理論上一致しうるかを確かめる。** 非決定性(同点・タイブレーク・浮動小数の丸め・辞書の走査順)が結果に入りうるなら、閾値は「一致する量」と「一致させるために揃える規則」に分けて書く。ai-trilogy HC-001(主張の向きを書く前に測る)の隣接規範だが、あちらは*主張*、こちらは*計器の到達可能性*を対象とする |
| 種別 | agents_md(**昇格済み** — agents_core に収録)|
| 効果検証 | loop_002 以降で O-1a / O-1b がそれぞれ独立に赤緑を返すこと。O-1b が落ちたときにタイブレーク規則の不一致として診断できれば Closed |
| propagation | **2026-08-31 フリート昇格済み → HC-073**(registry v1.22.0・全 66 プロジェクトへ展開)|
| 状態 | Verifying |

## HC-002(→ フリート HC-074)

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-30 |
| トリガー | `VERIF-FALSE` × 3(loop_001 文字種検査 / loop_004 G-01 の物差し / loop_004 T-030b) |
| 診断 | 三件とも**検査側が、検査対象の実際の性質を観測せずに書かれていた**。(1) 本文に δ・Σ・α が数式記号として出ることを見ずにギリシャ文字全域を禁止した。(2) ルビ基底の実例を見ずに「自動推定された基底 = 人間の区切り」と仮定した(実際は「処躊躇」のような非語が取れる)。(3) 実装が文字境界からしか `candidates()` を呼ばないことを見ずに、テストが全バイト位置で呼んで偽の候補を作った。AGENTS.md の HC-040(述語の射程も書く前に観測する)が既に同じことを言っているのに、三度とも踏んだ |
| 改訂 | 検査を書いたら、**対象に当てる前にまず正常系に当てて誤検出 0 を確かめる**。陰性対照を「作った例」で済ませず、**実データの正常な部分から取る**。とくに (a) 文字クラスや符号位置の範囲を禁止するとき (b) 外部データの構造に「これは○○を意味する」というラベルを与えるとき (c) 実装の内部関数をテストから直接呼ぶときは、実装がその関数をどう呼んでいるかを先に読む |
| 種別 | agents_md(**昇格済み** — agents_core に収録)|
| 効果検証 | loop_005 以降 5 ループで VERIF-FALSE の新規発生 0 件なら Closed |
| propagation | **2026-08-31 フリート昇格済み → HC-074**(registry v1.22.0・全 66 プロジェクトへ展開)|
| 状態 | Verifying |

## HC-003(→ フリート HC-075)

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-30 |
| トリガー | `GEN-LOGIC` × 3(loop_002 char.bin の文字種ビット / loop_004 外字 〓 / loop_004 ｜ 分岐の注記素通り) |
| 診断 | 三件とも**外部形式を「たいていこう」と仮定して書き、例外に当たった**。(1) `char.bin` の type ビットが単一カテゴリだと仮定したが KANJINUMERIC は KANJI と重なる。(2) 外字を 〓 に置換したのに 〓 を基底の文字クラスに入れ忘れた。(3) 青空文庫の `｜` 分岐が注記を素通りさせ、注記の生テキストが基底に入った。**三件とも述語ゲートが捕まえた** —— カテゴリ規則の一意性検算・残骸ゲート・基底の注記混入検査 |
| 改訂 | 外部形式(辞書バイナリ・注記記法・ファイル書式)を読むコードには、**「この仮定が崩れたら落ちる」検算をコードの中に置く**。`build_dict.py` の「posid が品詞四つ組を一意に決める」「カテゴリ規則が符号位置ごとに違わない」がその形で、実際に自分で落ちて教えてくれた。仮定を書いたら同じ行の近くに検算を置き、**黙って通る道を作らない** |
| 種別 | agents_md(**昇格済み** — agents_core に収録)|
| 効果検証 | 外部形式を読む新規コードすべてに検算が入っていること。loop_005 以降 5 ループで GEN-LOGIC の新規発生 0 件なら Closed |
| propagation | **2026-08-31 フリート昇格済み → HC-075**(registry v1.22.0・全 66 プロジェクトへ展開)|
| 状態 | Verifying |

## HC-004(→ フリート HC-076)

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-31 |
| トリガー | `TOOL-MISUSE` × 2(loop_002 fugashi の引数分割 / loop_009 コミット種別 `perf:`) |
| 診断 | どちらも**道具が受け付ける語彙を確かめずに使った**。とくに後者は害が大きい ── `looplog append` が `kind=perf` を拒否したのに、そのまま `loop_end` を記録してしまい、**LL-09 で追記できなくなって監査証跡に穴が空いた**(該当コミット d0868aa)。追記専用ログでは、拒否は「あとで直せる失敗」ではない |
| 改訂 | **`looplog append` が拒否されたら、その場で直すまで次へ進まない。** `loop_end` を書く前に (a) そのループで記録するつもりだった行がすべて入っているか (b) **`failure_count` を実際に数えたか**(`correction` で無効化した行は数えない)を確かめる。コミット種別は AGENTS.md の規約(feat/fix/test/docs/refactor/chore、拡張 data/spec)から出ない ── `perf` は Conventional Commits にはあるが、この規約には無い |
| 種別 | agents_md(**昇格済み** — agents_core に収録)|
| 効果検証 | loop_010 以降、各ループの `loop_end` 直前に記録の欠落と件数のずれが無いことを確かめる。**起票した直後の loop_009 で、今度は `failure_count` を数えずに書いて validate を落とした** ── 同じ型の二度目。TOOL-MISUSE の再発 0 件で Closed |
| propagation | **2026-08-31 フリート昇格済み → HC-076**(registry v1.22.0・全 66 プロジェクトへ展開)|
| 状態 | Verifying |

## HC-005(→ フリート HC-077)

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-31 |
| トリガー | `SPEC-DRIFT` × 2(loop_003 O-2 を「AST 静的検査」と書いた / loop_007 「Brotli でさらに下がる」) |
| 診断 | 二件とも**確かめていないことを断定形で SPEC に書いた**。片方は実装の説明(実際はコメント除去つきの字句走査)、もう片方は未測定の予測(実測は逆で、Brotli のほうが 1.25 倍大きかった)。どちらも文書同期の段で見つかったが、その前に読んだ人は誤った説明を信じる |
| 改訂 | SPEC に書く文は三つに分ける。(1) **実測**は値と実測日を添える。(2) **見込み**は「見込みである」と明示し、確かめる手段を併記する。(3) **実装の説明**は実物に当てて確かめてから書く ── 「AST で」「Brotli で」のような手段の名指しは、その手段が実際に使われていることを確認してから書く |
| 種別 | agents_md(**昇格済み** — agents_core に収録)|
| 効果検証 | loop_010 以降、SPEC の断定形が実測・見込み・実装説明のいずれかに分類できること。SPEC-DRIFT の再発 0 件で Closed |
| propagation | **2026-08-31 フリート昇格済み → HC-077**(registry v1.22.0・全 66 プロジェクトへ展開)|
| 状態 | Verifying |

## HC-006(→ フリート HC-078)

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-31 |
| トリガー | `VERIF-GAP` × 2(loop_005 ラティス図が 28 段の細長い柱 / loop_007 実機幅で表が潰れて 16,635px) |
| 診断 | 二件とも**見た目の破綻がテストも検品も緑のまま素通りした**。前者は同表層の別トークンを全部別段に置いたため、後者は `table { width:100% }` だけで狭い画面の列が潰れたため。どちらも「要素が在るか」「本文が出ているか」という検査では捕まらない ── **画面幅を変えて実際に見るまで分からない** |
| 改訂 | 図・表に手を入れたループの完了条件に、**複数の画面幅で実際に見る**を入れる。あわせて機械で捕まえられる代理指標を置く: `build/mobile_check.mjs` が (a) 横溢れ (b) **縦の伸びすぎ**(列が潰れて一文字ずつ折り返すとここで出る)を見る。代理指標は目視の代わりではなく、目視を忘れたときの網である |
| 種別 | agents_md(**昇格済み** — agents_core に収録)|
| 効果検証 | 図・表を触った以後のループで実機幅の確認が行われていること。VERIF-GAP の再発 0 件で Closed |
| propagation | **2026-08-31 フリート昇格済み → HC-078**(registry v1.22.0・全 66 プロジェクトへ展開)|
| 状態 | Verifying |
