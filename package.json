{
  "name": "kizami-ji",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "刻み路 — 形態素解析をラティス上の最小コスト経路探索として見せる",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
