# TEST_SPEC.md — kizami-ji

<!-- scaffold template v1.13.0 から展開(2026-08-30) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- 出荷側(TypeScript)は `vitest run`。両者が同じフィクスチャを読む
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

合成フィクスチャだけでなく、**外部データ・外部権威に対する期待値**にも同じ規律が要る。
ケースごとに、その期待値がどこから来たのかを書くこと。

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
外部データは重複や欠落を含むため、数はデータが動くたびに壊れる。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| MeCab の解析結果 | fugashi + `ipadic` 1.0.0 をビルド時に実行 | **外部権威**。出荷実装は一切参照しない(O-2) |
| `char.def` の文字種定義 | IPADIC 同梱 `char.def`(テキスト) | **外部権威**。`char.bin` からの復元と突き合わせる |
| lexsize / 表層数 | `sys.dic` ヘッダの `lexsize` | **コーパス自身が持つ件数オラクル**(HC-012) |
| ルビの括り | 青空文庫 raw のルビ注記(furigana-keiryo の抽出器) | **外部の物差し**。形態素解析を使わずに作る |
| 連接表シャッフル辞書 | `matrix.bin` を seed 固定で置換 | **陰性対照**(O-3) |

## ケース一覧

### 辞書の復元(F-01 の前提)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | §5 | `sys.dic` から復元したトークン総数 | ヘッダの `lexsize` と一致(定数で書かない) |
| T-002 | §5 | `char.bin` から復元した文字種定義 | IPADIC 同梱 `char.def` の全行と一致 |
| T-003 | §5 | 自前索引の共通接頭辞検索 | 全表層について、double-array の検索結果と**集合として一致** |
| T-004 | §5 | 出荷用に組み直した token 表 | 元の `sys.dic` の (lcAttr, rcAttr, posid, wcost) と全件一致 |

### 二実装照合(O-1a / O-1b)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-010 | O-1a | 青空文庫 10,000 文の**総コスト** | MeCab と 100.0000% 一致(不一致 0 件) |
| T-011 | O-1b | 同上の**分割** | MeCab と 100.0000% 一致。**タイブレーク規則を合わせた上で** |
| T-012 | O-1b | 完全同点の実例(「の北詰まで」) | 上位二経路の総コストが厳密に等しい。期待値は §6 の実測(2026-08-29) |
| T-013 | G-02 | 未知語を含む文の文字種判定 | 生成された未知語ノードの (カテゴリ, 範囲) が MeCab と完全一致 |
| T-014 | O-1a | TypeScript 実装と Python 実装 | 同一入力に対し分割・総コスト・δ が完全一致 |

### 陽性・陰性対照(HC-041 / HC-051)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-020 | O-3 | 連接表を seed 固定でシャッフルした辞書で T-010 を走らせる | **落ちること**。落ちなければ T-010 が何も検査していない |
| T-021 | O-2 | 出荷側ソースの字句走査(コメントを除去) | 参照実装の名指しと `probe/` `scripts/` からの import が 0 件 |
| T-022 | O-2 | T-021 の陽性対照 | 依存を実行時に注入した仮ソースを **T-021 が捕まえる**。符号位置から組み立て、原稿に直接書かない(ai-trilogy HC-002) |
| T-023 | O-2 | T-021 の走査対象が空でないこと | 走査したファイル数 > 0 |

### δ と外部の物差し(G-01)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-030 | F-04a | δ の定義 | 二位は一位と**分割が異なる**経路であること(同分割・同表層の別トークンは二位に数えない) |
| T-031 | G-01 | δ 下位四分位 / 上位四分位のルビ境界違反率 | 記録済みの測定値が閾値(比 1.5)に**届いていない**こと。**2026-08-30 の実測 1.253**。届くようになったら F-32b を見直す |
| T-032 | G-01 | 交絡検査(漢字連続の内部に限る) | 効果が反転していること(比 0.841)。全体のわずかな効果が漢字らしさの影であることの記録 |
| T-034 | F-32b | 出荷物の本文が δ を「危うさ」「間違いやすさ」と結びつけていない | 禁止語 0 件。**陽性対照つき**(符号位置から組み立てる) |
| T-033 | G-01 | ルビ抽出が形態素解析を使っていない | 字句走査で解析器への依存 0 件 |

### 痩せた辞書(F-10a / G-04)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-060 | G-04a | 無作為 1,000 語の境界 F1 と辞書ゼロの差 | 0.01 未満(**実測 0.0001**) |
| T-061 | G-04b | 頻度上位 1,000 語の境界 F1 − 辞書ゼロ | 0.15 以上(**実測 0.1834**) |
| T-062 | G-04c | 無作為辞書の語が実際に使われている | 使用トークン > 0(**実測 460 / 85,249**)。0 なら対照が空回り |
| T-063 | F-10a | 語数を減らすほど境界 F1 が下がる | 単調。崩れたら刈り方か測り方が壊れている |
| T-064 | F-10a | 画面が名指しする実例が焼いたデータのとおり | 完全辞書は「學生」を割り、300 語は一語に保つ。「首都」は途中で割れて戻る(HC-045) |
| T-065 | F-10a | 頻度を取る群と測る群が分かれている | 両群が十分な大きさ。重なりの検算は `measure_thin.py` が実行時に行い、あれば落ちる |

### 出荷物と費用(N-01〜N-05)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-040 | N-01 | ビルド成果物 | サーバ関数が 0 件 |
| T-041 | N-03 | 出荷物のファイル一覧 | `.wasm` が 0 件 |
| T-042 | N-07 | NOTICE | mecab-ipadic の著作権表示が本文として存在する |
| T-043 | N-04 / G-03 | 初回解析までの転送量 | 実ブラウザで実測し 8 MB 未満。**HEAD ではなく範囲つき GET で測る**(Vercel は HEAD に content-length を返さない) |
| T-044 | N-05 / F-06 | トップページの辞書取得 | 実ブラウザで 0 バイト |
| T-045 | N-02 | 実行時の外部通信 | 自オリジン以外へのリクエストが 0 件 |

### 本文の衛生(フリート共通)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-050 | — | **本文**の文字種検査 | キリル 0 件 / 許可リスト外のギリシャ 0 件(senoto-mori の text-hygiene 準拠)。走査は本文(`*.md` と `src/` `app/` `content/`)に限る ── ソースの文字クラス定義まで撃つと、ギリシャ・キリル基底のルビ(`γ《ガムマア》` `Л《エル》`)を表現できなくなる |  <!-- text-hygiene:allow -->
| T-051 | — | T-050 の陽性・陰性対照 | 符号位置から組み立てた混入例を捕まえ、正当な日本語を撃たない |
| T-051b | — | 除外の検算 | `scripts/ruby.py` のギリシャ・キリルが文字クラス定義か、その根拠のコメントに限られること。**定義行とコメント行の両方が実在すること**も確かめる(除外が「コメントなら何でも通る」に崩れない) |
| T-052 | F-11 | フッタ 5 項目 | 全ページに存在する |

## 目視検品(自動化しない — HC-041)

テストで代替せず、図を触ったループの完了条件に含める。

- ラティス図の候補ノードが、同じデータから出した解析結果と食い違っていないこと(HC-045)
- 二位の道の重ね描きが一位と入れ替わっていないこと
- DP の段階表示が、最終的な最小経路と矛盾しないこと
- 検品の道具自体が壊れていないこと — 取得に失敗した画面を撮っても「撮影しました」と出る。
  `build/page_check.mjs` は失敗を終了コードで知らせる(ai-trilogy 三部作の実装を踏襲)
- **複数の画面幅で実際に見る**(HC-006)。図・表に手を入れたループの完了条件。
  代理指標として `build/mobile_check.mjs` が横溢れと**縦の伸びすぎ**を見るが、
  これは目視の代わりではなく、目視を忘れたときの網である
